package br.ada.t1431.pix.domain;

/**
 * Enum que representa o status de uma chave Pix.
 */
public enum Status {
    /** Chave ativa. */
    ATIVO,
    /** Chave inativa. */
    INATIVO,
    /** Chave expirada. */
    EXPIRADA
}
